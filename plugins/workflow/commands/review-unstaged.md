---
description: Review unstaged changes for code quality, style, and potential issues
allowed-tools:
  - Agent
---

Launch the `review-unstaged` agent to review unstaged changes with context isolation.

Pass the following context to the agent:
- **Working directory**: the current project root

When the agent completes, report its findings to the user.
